# AegisRAG
This system acts as an automated "security auditor." It takes a company's internal policy documents, safety regulations, and compliance rules (the RAG knowledge base). It then automatically generates adversarial prompt attacks against a target AI model, retrieves relevant safety guidelines, and judges whether the model successfully defended itself.
